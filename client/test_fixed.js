const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:5173'); await page.setViewport({ width: 390, height: 844 }); const result = await page.evaluate(() => { function getBlock(element) { let parent = element.parentElement; while (parent && parent !== document.documentElement) { const style = window.getComputedStyle(parent); if (style.transform !== 'none' || style.perspective !== 'none' || (style.willChange && style.willChange.includes('transform')) || (style.filter && style.filter !== 'none')) { return style.transform + ' / ' + style.filter; } parent = parent.parentElement; } return null; } const nav = document.querySelector('nav'); return nav ? getBlock(nav) : 'No nav found'; }); console.log('BLOCKER:', result); await browser.close(); })();
